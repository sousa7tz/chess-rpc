# Optional: Only configure this file if you know what you are doing and want to use a custom Discord Application.
# Default values will be used automatically if left empty.

# Discord Application Client ID (defaults to the pre-configured project app)
DISCORD_CLIENT_ID=

# Local WebSocket server port (default: 3020)
PORT=3020